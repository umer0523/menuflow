/** User-facing copy for the location switcher — no hardcoded strings in components. */
export const LOCATION_SWITCHER = {
  LOADING_LABEL: 'Loading locations',
  EMPTY: 'No locations available.',
  GROUP_LABEL: 'Select location',
} as const;
