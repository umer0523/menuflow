/**
 * Square environments MenuFlow supports. Sandbox-only by project scope —
 * production is intentionally excluded so a real merchant token can never be used.
 */
export enum SquareEnvironment {
  Sandbox = 'sandbox',
}
