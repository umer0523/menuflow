/** OpenAPI document metadata + the path the browsable UI is mounted at. */
export const SWAGGER = {
  PATH: 'docs',
  TITLE: 'MenuFlow API',
  DESCRIPTION: 'Backend proxy over Square — locations, catalog, categories, and items.',
  VERSION: '1.0',
} as const;
